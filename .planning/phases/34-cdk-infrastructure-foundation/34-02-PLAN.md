---
phase: 34-cdk-infrastructure-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - infra/cdk/lib/prediction-stack.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-05

must_haves:
  truths:
    - "cdk synth produces a CloudFormation template with no errors or warnings"
    - "Template diff shows only creates (no updates or deletes) for a fresh deployment"
    - "User has confirmed CDK bootstrap is complete and cdk deploy succeeds"
    - "All CfnOutputs are emitted (InstanceId, EcrRepoUri, SecretArn, LogGroupName, VpcId)"
  artifacts:
    - path: "infra/cdk/cdk.out/PredictionStack.template.json"
      provides: "Synthesized CloudFormation template"
      contains: "AWS::EC2::Instance"
  key_links:
    - from: "cdk synth output"
      to: "AWS CloudFormation"
      via: "cdk deploy"
      pattern: "PredictionStack"
---

<objective>
Validate the synthesized CloudFormation template for correctness and completeness, then guide the user through CDK bootstrap and first deployment.

Purpose: Ensure the CDK stack is correct before deploying real AWS resources. The user must bootstrap CDK (one-time) and tear down existing manual infrastructure before deploying.
Output: Validated template ready for deployment, user-verified deployment.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-cdk-infrastructure-foundation/34-01-SUMMARY.md
@.planning/phases/34-cdk-infrastructure-foundation/34-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate synthesized CloudFormation template</name>
  <files>infra/cdk/lib/prediction-stack.ts</files>
  <action>
Run `cd infra/cdk && npx cdk synth > /dev/null` to produce the template, then validate the synthesized output in `cdk.out/PredictionStack.template.json`:

1. **Resource count check:** Confirm these resource types exist in the template:
   - `AWS::EC2::VPC` (exactly 1)
   - `AWS::EC2::Subnet` (exactly 1, public)
   - `AWS::EC2::InternetGateway` (exactly 1)
   - `AWS::EC2::SecurityGroup` (exactly 1, no ingress rules)
   - `AWS::EC2::Instance` (exactly 1, with 2 BlockDeviceMappings)
   - `AWS::IAM::Role` (at least 1)
   - `AWS::IAM::InstanceProfile` (exactly 1)
   - `AWS::Logs::LogGroup` (exactly 1, RetentionInDays: 14)
   - `AWS::SecretsManager::Secret` (exactly 1)

2. **Absence check:** Confirm these do NOT exist:
   - `AWS::ECR::Repository` (ECR is imported, not created)
   - `AWS::EC2::NatGateway` (public subnet only, no NAT)
   - `AWS::EC2::EIP` (not in scope for Phase 34 -- can add later)

3. **EBS validation:** Find the Instance resource, confirm BlockDeviceMappings includes `/dev/xvdf` with `DeleteOnTermination: false`.

4. **IAM validation:** Check the Role resource has managed policy ARNs for SSM and AMP, and inline policy statements granting ECR, Secrets, and Logs access.

5. **Fix any issues found.** If the template is missing resources or has incorrect configuration, update `prediction-stack.ts` and re-synth until clean.

Run `npx cdk diff` (this will fail if not bootstrapped/deployed, which is expected -- but it shows what WOULD be created). Capture the diff output showing all resources as "will be created".
  </action>
  <verify>
`npx cdk synth` exits 0. Template JSON contains all required resources and none of the excluded resources. EBS data volume has DeleteOnTermination: false.
  </verify>
  <done>Synthesized template validated: all 6 INFRA requirements present, no unwanted resources, EBS persistence confirmed, IAM least-privilege confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy CDK stack and verify AWS resources</name>
  <files>infra/cdk/lib/prediction-stack.ts</files>
  <action>
CHECKPOINT: Human deploys the CDK stack and verifies AWS resources are created correctly.

What was built: Complete CDK infrastructure stack provisioning VPC (public subnet, no NAT), security group (egress only), EC2 t3.small with Amazon Linux 2023, 30 GB EBS data volume (persists on termination), IAM instance profile (least-privilege), CloudWatch log group (14-day retention), Secrets Manager secret shell, and ECR import.

Steps for the user:

1. Tear down existing manual infrastructure (STATE.md blocker):
   - Terminate the existing EC2 instance (if running)
   - Delete existing security groups (except default)
   - Delete existing VPC (if manually created)
   - Do NOT delete the ECR repository -- CDK imports it

2. Bootstrap CDK (one-time): `cd infra/cdk && npx cdk bootstrap aws://606103597377/us-east-1`

3. Deploy: `cd infra/cdk && npx cdk deploy --require-approval broadening`

4. Verify outputs: InstanceId, EcrRepoUri, SecretArn, LogGroupName in stack outputs

5. Populate secrets: `aws secretsmanager put-secret-value --secret-id prediction/prod/credentials --secret-string '{"DERIBIT_CLIENT_ID":"real","DERIBIT_CLIENT_SECRET":"real","DERIVE_WALLET_KEY":"real"}'`

6. Idempotency test: `npx cdk diff` should show no changes
  </action>
  <verify>User confirms `cdk deploy` succeeded and stack outputs are emitted. EC2 instance is running, Secrets Manager secret exists, CloudWatch log group exists.</verify>
  <done>CDK stack deployed to AWS. All foundational resources created. Stack outputs recorded for Phase 35+.</done>
</task>

</tasks>

<verification>
1. CloudFormation template validates with all required resources
2. No excluded resources (ECR create, NAT gateway) in template
3. User confirms successful `cdk deploy` with all outputs emitted
4. EC2 instance is running in AWS Console
5. Secrets Manager secret exists with placeholder values
6. CloudWatch log group exists with 14-day retention
</verification>

<success_criteria>
- CDK stack deployed successfully to AWS account 606103597377 / us-east-1
- All 6 INFRA requirements verified in live AWS environment
- Stack outputs recorded for use in subsequent phases (35+)
- Existing ECR repository preserved with all images intact
</success_criteria>

<output>
After completion, create `.planning/phases/34-cdk-infrastructure-foundation/34-02-SUMMARY.md`
</output>
