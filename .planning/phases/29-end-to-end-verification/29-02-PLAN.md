---
phase: 29-end-to-end-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/signal_scoring_e2e.rs
autonomous: true
requirements: [SIGNAL-01, SIGNAL-02, SIGNAL-03, SIGNAL-04, SIGNAL-05]

must_haves:
  truths:
    - "signal-scoring binary produces correct hit rates for a known settlement dataset"
    - "signal-scoring binary produces correct Sharpe ratio for a known P&L series"
    - "signal-scoring binary produces correct drawdown metrics for a known P&L series"
    - "signal-scoring binary handles empty settlement data gracefully without panics"
    - "signal-scoring binary handles malformed JSONL lines gracefully with warning count"
    - "signal-scoring --output json produces valid parseable JSON matching table content"
  artifacts:
    - path: "tests/signal_scoring_e2e.rs"
      provides: "End-to-end integration tests for signal-scoring CLI binary"
      min_lines: 200
  key_links:
    - from: "tests/signal_scoring_e2e.rs"
      to: "src/bin/signal_scoring.rs"
      via: "std::process::Command invoking compiled binary"
      pattern: "Command::new.*signal.scoring"
    - from: "tests/signal_scoring_e2e.rs"
      to: "src/paper_trade/analyzer.rs"
      via: "AnalysisSettlementRecord struct serialization for fixture generation"
      pattern: "serde_json::to_string.*AnalysisSettlementRecord"
---

<objective>
End-to-end verification of the signal-scoring CLI binary using synthetic golden-value test fixtures.

Purpose: Prove that signal-scoring produces correct, trustworthy output by testing against hand-computed expected values for hit rate, edge significance, Sharpe ratio, PSR, and drawdown. This cross-cuts SIGNAL-01 (hit rates + Wilson CI), SIGNAL-02 (edge t-test), SIGNAL-03 (Sharpe), SIGNAL-04 (PSR), and SIGNAL-05 (drawdown).

Output: tests/signal_scoring_e2e.rs with 5-6 integration tests covering golden values (JSON output parsed and asserted with epsilon), table output content verification, empty range handling, malformed JSONL tolerance, and single-record degenerate case.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-end-to-end-verification/29-RESEARCH.md
@src/bin/signal_scoring.rs
@src/paper_trade/analyzer.rs
@src/analysis/scoring.rs
@src/analysis/io.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create signal-scoring golden value and edge case E2E tests</name>
  <files>tests/signal_scoring_e2e.rs</files>
  <action>
Create `tests/signal_scoring_e2e.rs` with end-to-end integration tests for the signal-scoring CLI binary.

**Helper function:** Create a `cargo_bin(name: &str) -> PathBuf` helper that resolves the binary path from `std::env::current_exe()` parent chain. On Windows, set extension to "exe".

**Fixture generation:** Create a `make_settlement(event_id, gross_hit, net_hit, pnl, settled_ms) -> AnalysisSettlementRecord` helper that builds an AnalysisSettlementRecord struct with the given fields and sensible defaults (position_id from settled_ms, venue_pair="polymarket-kalshi", pattern="YES_NO", threshold_status=Some(PassedBoth), convergence_secs=300.0, total_fees="0.00", total_slippage="0.00", inter_leg_gap_ms=Some(100), stale_fill=false, running metrics all 0.0). CRITICAL: `total_raw_pnl` and `total_net_pnl` are String fields (not f64), so pass them as string values like "1.00" and "-0.50". Serialize each record with `serde_json::to_string()` and write to a JSONL file named `settlements-{YYYY-MM-DD}.jsonl` (NOTE the "settlements-" prefix) inside a tempdir.

**Test 1: `golden_value_hit_rates_json`** -- Create 10 AnalysisSettlementRecord records with:
  - 7 gross_hit=true, 3 gross_hit=false
  - 5 net_hit=true, 5 net_hit=false
  - PnL values: ["1.00", "-0.50", "0.80", "-0.30", "1.20", "-0.10", "0.60", "0.40", "-0.70", "0.90"]
  - All settled on 2026-01-15 with timestamps spread across the day (e.g., 1769472000000 + i*3600000)
Run `signal-scoring --from 2026-01-15 --to 2026-01-15 --output json --settlement-dir {tmpdir}`. Parse stdout as JSON. Assert:
  - hit_rates.gross_rate ~= 0.70 (epsilon 1e-4)
  - hit_rates.net_rate ~= 0.50
  - hit_rates.total == 10
  - Process exits with status success

**Test 2: `golden_value_edge_and_sharpe_json`** -- Using same 10-record fixture. Parse JSON output. Assert:
  - edge_test.mean_edge ~= 0.33 (mean of [1.0, -0.5, 0.8, -0.3, 1.2, -0.1, 0.6, 0.4, -0.7, 0.9] = 3.3/10 = 0.33)
  - edge_test.n == 10
  - sharpe.per_trade_sharpe is present and > 0 (positive mean / positive stddev)
  - sharpe.n == 10
  - sharpe.psr is present (not null) since n >= 3

**Test 3: `golden_value_drawdown_json`** -- Using same 10-record fixture. Parse JSON output. Assert:
  - drawdown is present (not null)
  - drawdown.max_drawdown_abs > 0.0 (the P&L series has drawdowns)
  - drawdown.peak_date is a non-empty string
  - drawdown.trough_date is a non-empty string

**Test 4: `table_output_contains_scoring_sections`** -- Run with default `--output table` using same fixture. Assert stdout contains "HIT RATES", "COST-ADJUSTED EDGE", "SHARPE RATIO", and "MAXIMUM DRAWDOWN" section headers. Assert exit code 0.

**Test 5: `empty_range_no_positions`** -- Create an empty tempdir (no JSONL files). Run with `--from 2099-01-01 --to 2099-01-02 --settlement-dir {tmpdir}`. Assert exit code 0. Assert stdout contains "No settled positions in range".

**Test 6: `malformed_lines_skipped_with_warning`** -- Write a JSONL file named `settlements-2026-01-15.jsonl` containing 3 valid AnalysisSettlementRecord lines and 1 line of garbage text. Run with `--from 2026-01-15 --to 2026-01-15 --output json --settlement-dir {tmpdir}`. Assert exit code 0. Assert stderr contains "malformed JSONL" or "Warning". Assert JSON output shows hit_rates.total == 3.

**Test 7: `single_record_degenerate_case`** -- Write 1 valid settlement record. Run with `--output json`. Assert:
  - hit_rates is present (n=1 is valid for hit rates)
  - edge_test is null/None (n=1 means stddev=0, t-test undefined)
  - sharpe is null/None (n=1, stddev=0)
  - Exit code 0 (no panics)

**Important implementation notes:**
- Use `tempfile::tempdir()` for each test (already in dev-dependencies)
- Use `f64::abs()` with epsilon for floating-point comparisons, NEVER exact equality
- For JSON assertions, parse as `serde_json::Value` and navigate with indexing
- The signal-scoring binary uses `--settlement-dir` (not `--settlement_dir`) on the command line
- Settlement file naming is `settlements-{YYYY-MM-DD}.jsonl` (WITH "settlements-" prefix)
- signal-scoring prints loading summary to stderr, scoring output to stdout
- "No settled positions" message goes to stdout (not stderr)
- Timestamps for 2026-01-15 00:00 UTC = 1769472000000
- total_raw_pnl and total_net_pnl are String fields, not numbers
- Import ThresholdStatus from `prediction::signal::types::ThresholdStatus`
  </action>
  <verify>
Run `cargo build --bins && cargo test --test signal_scoring_e2e -- --nocapture` and confirm all 7 tests pass. Zero panics, zero compilation errors.
  </verify>
  <done>
All 7 signal-scoring E2E tests pass: golden value hit rates match hand-computed values within epsilon, edge test and Sharpe ratio are present and reasonable, drawdown metrics are populated, table output contains all four scoring section headers, empty ranges produce graceful "No settled positions" message, malformed lines are skipped with warning while valid records still process, and single-record degenerate case produces hit rates without panicking on undefined stddev.
  </done>
</task>

</tasks>

<verification>
1. `cargo build --bins` compiles both CLI binaries without errors
2. `cargo test --test signal_scoring_e2e` runs all tests successfully
3. Golden value assertions use epsilon comparisons (1e-4 to 1e-6), not exact equality
4. Each test uses its own tempdir (no shared state, parallel-safe)
5. No hardcoded paths to `spread_logs/` or `settlement_logs/` directories
6. Single-record degenerate case proves no division-by-zero panics
</verification>

<success_criteria>
- 7 E2E tests for signal-scoring pass covering hit rates (SIGNAL-01), edge t-test (SIGNAL-02), Sharpe ratio (SIGNAL-03), PSR (SIGNAL-04), drawdown (SIGNAL-05), table output format, empty range edge case, malformed JSONL edge case, and single-record degenerate case
- Tests are self-contained with synthetic fixtures (no dependency on real soak test data)
- Hand-verified golden values for hit rate, mean edge match CLI output
</success_criteria>

<output>
After completion, create `.planning/phases/29-end-to-end-verification/29-02-SUMMARY.md`
</output>
