---
phase: 13-phase4-verification-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feed/traits.rs
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "NormalizedDataSource trait is removed from src/feed/traits.rs"
    - "cargo build succeeds with no compilation errors after removal"
    - "cargo test passes with no regressions after removal"
    - "TEST-01 is satisfied by the existing RawDataSource trait-based abstraction (SyntheticDataSource + ReplayDataSource)"
  artifacts:
    - path: "src/feed/traits.rs"
      provides: "RawDataSource trait, RecordLine struct, Recorder trait (NormalizedDataSource removed)"
      contains: "pub trait RawDataSource"
  key_links:
    - from: "src/feed/traits.rs"
      to: "src/feed/mock/synthetic.rs"
      via: "SyntheticDataSource implements RawDataSource"
      pattern: "impl.*RawDataSource.*for.*SyntheticDataSource"
    - from: "src/feed/traits.rs"
      to: "src/feed/mock/replay.rs"
      via: "ReplayDataSource implements RawDataSource"
      pattern: "impl.*RawDataSource.*for.*ReplayDataSource"
---

<objective>
Remove the NormalizedDataSource dead trait from src/feed/traits.rs and verify TEST-01 satisfaction through the existing RawDataSource abstraction.

Purpose: The v1.0 audit identified NormalizedDataSource as dead code with zero implementations. Removing it resolves the audit finding while the existing RawDataSource trait (with SyntheticDataSource and ReplayDataSource implementations) already satisfies TEST-01's requirement for a "mock data layer via trait-based abstraction."

Output: Updated `src/feed/traits.rs` with dead code removed; compilation and test confirmation.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-phase4-verification-cleanup/13-RESEARCH.md

# Source file to modify
@src/feed/traits.rs

# Mock infrastructure that satisfies TEST-01 (read to confirm)
@src/feed/mock/synthetic.rs
@src/feed/mock/replay.rs
@src/feed/pipeline.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove NormalizedDataSource dead code and verify TEST-01</name>
  <files>src/feed/traits.rs</files>
  <action>
**Step 1: Pre-removal verification**
Run `grep -rn "NormalizedDataSource" src/` to confirm zero usages beyond the definition at traits.rs:40.

**Step 2: Remove the dead trait**
Edit `src/feed/traits.rs` to remove lines 35-45 (the NormalizedDataSource trait definition and its doc comment):
```rust
// REMOVE these lines:
/// Normalized data source.
///
/// Produces `MarketSnapshot` directly, bypassing WS parsing.
/// Used for testing downstream consumers in isolation from the
/// venue-specific parsing and normalization layers.
pub trait NormalizedDataSource: Send + 'static {
    /// Start the data source and return a receiver for normalized snapshots.
    fn start(
        &self,
    ) -> impl std::future::Future<Output = anyhow::Result<mpsc::Receiver<MarketSnapshot>>> + Send;
}
```

After removal, there should be no blank line gap -- the `RecordLine` struct doc comment should follow directly after the `RawDataSource` trait closing brace with one blank line separator.

Also check if `MarketSnapshot` is still needed in the imports at line 4 (`use crate::types::{DualTimestamp, MarketSnapshot, Venue};`). Since `MarketSnapshot` is no longer used in this file after removing NormalizedDataSource, remove it from the import:
- Change `use crate::types::{DualTimestamp, MarketSnapshot, Venue};`
- To `use crate::types::{DualTimestamp, Venue};`

BUT FIRST: Check if MarketSnapshot is used elsewhere in the file (e.g., in RecordLine or Recorder). If it IS used elsewhere, keep it in the import. Based on the current file content, MarketSnapshot is only referenced by NormalizedDataSource, so it should be safe to remove from the import.

**Step 3: Verify compilation**
Run `cargo build 2>&1` -- must succeed with zero errors. If removing MarketSnapshot from imports causes a compilation error, add it back.

**Step 4: Verify tests**
Run `cargo test 2>&1 | tail -20` -- all existing tests must pass with zero regressions.

**Step 5: Verify TEST-01 satisfaction**
Confirm that the mock data layer is functional by verifying:
- `RawDataSource` trait exists in traits.rs (the primary abstraction)
- `SyntheticDataSource` implements RawDataSource (grep for `impl.*RawDataSource.*for.*SyntheticDataSource` in synthetic.rs)
- `ReplayDataSource` implements RawDataSource (grep for `impl.*RawDataSource.*for.*ReplayDataSource` in replay.rs)
- `DataMode::Mock` in pipeline.rs runs the full pipeline without live connections
- All test_count tests pass including pipeline integration tests

This confirms TEST-01 is satisfied: the system has a trait-based abstraction (RawDataSource) with two implementations (SyntheticDataSource for mock, ReplayDataSource for replay) enabling full pipeline execution without live venue connections.
  </action>
  <verify>
1. `grep -rn "NormalizedDataSource" src/` returns zero results
2. `cargo build` succeeds with no errors
3. `cargo test` passes with same test count as before removal (no regressions)
4. `grep "pub trait RawDataSource" src/feed/traits.rs` returns a match (RawDataSource still present)
5. `grep "impl.*RawDataSource" src/feed/mock/synthetic.rs` returns SyntheticDataSource impl
6. `grep "impl.*RawDataSource" src/feed/mock/replay.rs` returns ReplayDataSource impl
  </verify>
  <done>
NormalizedDataSource trait is removed from traits.rs. Compilation succeeds with zero errors. All tests pass with zero regressions. TEST-01 is satisfied through RawDataSource trait with SyntheticDataSource (mock) and ReplayDataSource (replay) implementations, enabling full pipeline execution without live connections.
  </done>
</task>

</tasks>

<verification>
1. `NormalizedDataSource` string does not appear in any source file under `src/`
2. `cargo build` succeeds
3. `cargo test` passes with zero regressions
4. `RawDataSource` trait remains intact with SyntheticDataSource and ReplayDataSource implementations
5. `DataMode::Mock` pipeline path is unaffected
</verification>

<success_criteria>
- NormalizedDataSource trait removed from src/feed/traits.rs
- Unused MarketSnapshot import removed from traits.rs (if safe)
- cargo build succeeds
- cargo test passes with zero regressions
- TEST-01 satisfied: RawDataSource abstraction enables mock/replay pipeline without live connections
</success_criteria>

<output>
After completion, create `.planning/phases/13-phase4-verification-cleanup/13-02-SUMMARY.md`
</output>
