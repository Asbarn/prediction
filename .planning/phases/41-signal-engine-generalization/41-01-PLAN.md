---
phase: 41-signal-engine-generalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pricing/types.rs
  - src/pricing/engine.rs
  - src/signal/engine.rs
autonomous: true
requirements:
  - SIG-01
  - SIG-02
  - SIG-03
must_haves:
  truths:
    - "ImpliedProbability carries source_venue (Deribit or Derive) from PricingEngine through to CrossAssetEngine"
    - "CrossAssetEngine produces ArbSignal with correct options_leg.venue matching the source options venue"
    - "CrossAssetEngine generates signals when only Polymarket data exists (no Kalshi required)"
    - "Derive-sourced implied probabilities produce correctly attributed ArbSignals"
  artifacts:
    - path: "src/pricing/types.rs"
      provides: "ImpliedProbability with source_venue field"
      contains: "pub source_venue: Venue"
    - path: "src/pricing/engine.rs"
      provides: "PricingEngine populates source_venue from snapshot"
      contains: "source_venue: snapshot.venue"
    - path: "src/signal/engine.rs"
      provides: "CrossAssetEngine uses prob.source_venue for registry lookup and signal attribution; dynamic prediction venue iteration"
      contains: "prob.source_venue"
  key_links:
    - from: "src/pricing/engine.rs"
      to: "src/pricing/types.rs"
      via: "ImpliedProbability construction with source_venue"
      pattern: "source_venue: snapshot\\.venue"
    - from: "src/signal/engine.rs"
      to: "src/pricing/types.rs"
      via: "Reading prob.source_venue for registry lookup and signal output"
      pattern: "prob\\.source_venue"
---

<objective>
Remove hardcoded venue references from CrossAssetEngine so it correctly generates arbitrage signals using options-implied probabilities from any venue (Deribit or Derive) paired with any single prediction market (Polymarket alone, without requiring Kalshi).

Purpose: Currently CrossAssetEngine assumes all implied probabilities come from Deribit and requires both Polymarket and Kalshi data. This blocks signal generation for Derive-sourced probabilities and for markets with only one prediction venue.

Output: Three modified source files with venue-generic signal generation and updated tests.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-signal-engine-generalization/41-RESEARCH.md
@src/pricing/types.rs
@src/pricing/engine.rs
@src/signal/engine.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source_venue to ImpliedProbability and wire through production code</name>
  <files>src/pricing/types.rs, src/pricing/engine.rs, src/signal/engine.rs</files>
  <action>
**SIG-01: Add source_venue field to ImpliedProbability (src/pricing/types.rs)**

1. Add `use crate::types::Venue;` import at the top of `src/pricing/types.rs`.
2. Add a new field to the `ImpliedProbability` struct after the existing fields:
   ```rust
   /// Source options venue that produced this probability.
   pub source_venue: Venue,
   ```

**SIG-01: Populate source_venue in PricingEngine (src/pricing/engine.rs)**

3. At line ~389 (normal pricing path), add `source_venue: snapshot.venue,` to the `ImpliedProbability { ... }` construction.
4. At line ~494 (near-expiry path), add `source_venue: snapshot.venue,` to the `ImpliedProbability { ... }` construction.
   The `snapshot` variable is the incoming `MarketSnapshot` which already carries `.venue`.

**SIG-02: Use source_venue in CrossAssetEngine (src/signal/engine.rs)**

5. At line ~251, change `Venue::Deribit` to `prob.source_venue` in the `reg.lookup_by_instrument()` call.
6. At line ~256-258, update the warning log text from `"unmapped Deribit instrument"` to `"unmapped options instrument"` (or include `prob.source_venue` in the log for clarity).
7. At line ~543, change `venue: Venue::Deribit` to `venue: prob.source_venue` in the `options_leg: LegInfo { ... }` construction.

**SIG-03: Dynamic prediction venue iteration (src/signal/engine.rs)**

8. At line ~273, replace `for venue in [Venue::Polymarket, Venue::Kalshi]` with dynamic iteration over prediction venues that have cached data for this event_id:
   ```rust
   let pred_venues: Vec<Venue> = self.latest_pred.keys()
       .filter(|(eid, _)| eid == &event_id)
       .map(|(_, v)| *v)
       .collect();
   for venue in pred_venues {
       self.compute_and_emit(&event_id, venue, signal_tx).await;
   }
   ```
   Do NOT change the prediction venue filter at line ~292 (`if snap.venue != Venue::Polymarket && snap.venue != Venue::Kalshi`) -- that correctly categorizes which venues are prediction markets.

**Fix test helper in signal/engine.rs:**

9. Find the `make_implied_probability()` test helper (around line ~664-675) and add `source_venue: Venue::Deribit` to its `ImpliedProbability { ... }` construction.

**Anti-patterns to avoid:**
- Do NOT add venue-specific match arms in CrossAssetEngine -- the goal is removing venue specificity.
- Do NOT rename `deribit_taker_fee_rate` config field -- keep as-is per research recommendation (Option B).
- Do NOT change the `latest_prob` cache key to `(event_id, venue)` -- single options source per event is fine for v1.7.
  </action>
  <verify>
Run `cargo build` -- must compile with zero errors. Run `cargo test` -- all existing tests must pass (no regressions from the new field).
  </verify>
  <done>
ImpliedProbability has source_venue field. PricingEngine populates it from snapshot.venue at both construction sites. CrossAssetEngine uses prob.source_venue for registry lookup (line ~251) and signal attribution (line ~543). Prediction venue iteration is dynamic based on cached data. All existing tests compile and pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test coverage for venue-generic signal generation</name>
  <files>src/signal/engine.rs</files>
  <action>
Add three new test cases in the existing `#[cfg(test)]` module of `src/signal/engine.rs`:

1. **Test: Derive-sourced probability produces correctly attributed signal**
   - Create an ImpliedProbability with `source_venue: Venue::Derive` and a Derive instrument ID.
   - Set up an event mapping with a Derive instrument entry.
   - Feed the probability through handle_probability with a cached Polymarket prediction snapshot.
   - Assert the resulting ArbSignal has `options_leg.venue == Venue::Derive`.

2. **Test: Single prediction venue (Polymarket only) generates signal**
   - Create an ImpliedProbability (source_venue: Venue::Deribit).
   - Cache ONLY a Polymarket prediction snapshot (no Kalshi data at all).
   - Feed the probability through handle_probability.
   - Assert exactly one ArbSignal is emitted (from Polymarket pair, not from Kalshi).

3. **Test: Registry lookup uses correct source venue**
   - Create an ImpliedProbability with `source_venue: Venue::Derive`.
   - Set up an event mapping that has a Derive instrument but NOT a Deribit instrument for the same event.
   - Feed the probability -- should find the mapping via Derive venue.
   - With `source_venue: Venue::Deribit` for the same instrument -- should NOT find the mapping (returns None, logs unmapped).

Follow the existing test patterns in the file (use `make_implied_probability()` helper where possible, use `tokio::sync::mpsc` channels for signal_tx, use the existing test setup patterns for registry and engine).

If `make_implied_probability()` doesn't accept a venue parameter, either:
- Add an optional venue parameter, or
- Create the ImpliedProbability struct directly in the new tests with the desired source_venue.
  </action>
  <verify>
Run `cargo test -p prediction signal::engine` (or the appropriate test filter for signal engine tests) -- all new tests pass. Run `cargo test` -- full test suite passes with zero failures.
  </verify>
  <done>
Three new tests exist covering: (1) Derive-sourced probability attribution, (2) single prediction venue signal generation, (3) registry lookup with correct source venue. All tests pass. Full test suite green.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` compiles with zero errors and zero warnings related to the changed files
2. `cargo test` full suite passes -- no regressions
3. `grep -n "Venue::Deribit" src/signal/engine.rs` shows NO remaining hardcoded Deribit references in production code (test code may still use it as a valid test value)
4. `grep -n "source_venue" src/pricing/types.rs src/pricing/engine.rs src/signal/engine.rs` shows the field defined, populated in 2 places, and used in 2+ places
5. `grep -n "for venue in \[Venue::Polymarket" src/signal/engine.rs` returns NO matches (hardcoded list replaced with dynamic iteration)
</verification>

<success_criteria>
- ImpliedProbability struct has source_venue: Venue field
- PricingEngine sets source_venue from snapshot.venue at both construction sites
- CrossAssetEngine uses prob.source_venue (not Venue::Deribit) for registry lookup and signal attribution
- CrossAssetEngine iterates prediction venues dynamically from cache (not hardcoded list)
- Three new tests verify Derive attribution, single-venue operation, and venue-specific registry lookup
- Full cargo test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/41-signal-engine-generalization/41-01-SUMMARY.md`
</output>
