---
phase: 43-e2e-production-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - infra/cdk/lib/prediction-stack.ts
autonomous: true
requirements:
  - VER-02
must_haves:
  truths:
    - "signal_logs directory is mounted as a Docker volume so JSONL logs persist across container restarts"
    - "CDK user-data creates signal_logs data subdirectory on EC2"
  artifacts:
    - path: "docker-compose.yml"
      provides: "signal_logs volume mount for local/production docker-compose"
      contains: "signal_logs:/app/signal_logs"
    - path: "infra/cdk/lib/prediction-stack.ts"
      provides: "signal_logs volume mount in CDK-generated docker-compose and data directory creation"
      contains: "signal_logs"
  key_links:
    - from: "docker-compose.yml"
      to: "/app/signal_logs inside container"
      via: "Docker volume mount"
      pattern: "signal_logs:/app/signal_logs"
    - from: "infra/cdk/lib/prediction-stack.ts"
      to: "docker-compose.yml on EC2"
      via: "CDK user-data heredoc"
      pattern: "signal_logs"
---

<objective>
Fix the missing signal_logs Docker volume mount so that ArbSignal JSONL logs persist to the EC2 host filesystem.

Purpose: Without this mount, signal JSONL files written to `/app/signal_logs` inside the container are lost on every container restart or redeploy. VER-02 requires inspecting these logs on the host.
Output: Updated docker-compose.yml and CDK prediction-stack.ts with signal_logs volume mount.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-e2e-production-verification/43-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add signal_logs volume mount to docker-compose.yml and CDK</name>
  <files>docker-compose.yml, infra/cdk/lib/prediction-stack.ts</files>
  <action>
Fix the missing signal_logs Docker volume mount in two places:

1. **docker-compose.yml** (line ~15): Add `- /opt/prediction/data/signal_logs:/app/signal_logs` to the prediction service volumes, right after the existing `spread_logs` line. The pattern matches every other data directory already mounted.

2. **infra/cdk/lib/prediction-stack.ts** - TWO changes:
   a. **Data directory creation** (line ~195): Add `signal_logs` to the `mkdir -p /opt/prediction/data/{config,spread_logs,settlement_logs,paper_trades,state,logs}` command. Change it to `mkdir -p /opt/prediction/data/{config,spread_logs,signal_logs,settlement_logs,paper_trades,state,logs}`.
   b. **Docker-compose volumes** (line ~352): Add `'      - /opt/prediction/data/signal_logs:/app/signal_logs',` after the existing `spread_logs` volume mount line.

Do NOT change any other volumes or configuration. This is a targeted fix for the one missing mount.
  </action>
  <verify>
Verify the changes:
1. `grep "signal_logs" docker-compose.yml` shows the new volume mount line
2. `grep "signal_logs" infra/cdk/lib/prediction-stack.ts` shows both the mkdir and volume mount lines (should match 2 lines)
3. `cargo build` still compiles (no Rust changes, but confirm nothing is broken)
  </verify>
  <done>
signal_logs volume mount present in both docker-compose.yml and CDK prediction-stack.ts (mkdir + docker-compose volumes). Cargo build passes.
  </done>
</task>

</tasks>

<verification>
- `grep -c "signal_logs" docker-compose.yml` returns 1
- `grep -c "signal_logs" infra/cdk/lib/prediction-stack.ts` returns 2 (mkdir + volume)
- Volume mount line follows exact same pattern as spread_logs mount
</verification>

<success_criteria>
signal_logs Docker volume mount added to both local docker-compose.yml and CDK-generated docker-compose, matching the existing spread_logs pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/43-e2e-production-verification/43-01-SUMMARY.md`
</output>
