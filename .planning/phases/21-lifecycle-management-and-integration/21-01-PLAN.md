---
phase: 21-lifecycle-management-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/events.rs
  - src/events/toml_writer.rs
  - config/events.toml
autonomous: true
requirements: [LIFE-03, LIFE-01, LIFE-02]

must_haves:
  truths:
    - "LifecycleStatus::Retired variant exists and serializes to 'retired' via serde rename_all"
    - "DiscoveryConfig has archive_retention_days field with default 30"
    - "toml_writer can identify entries to archive (expired/retired older than retention) and unapproved past expiry"
    - "toml_writer can remove entries from a DocumentMut by ID using retain()"
    - "toml_writer can append archived entries to an archive document with status=retired and archived_at timestamp"
  artifacts:
    - path: "src/config/events.rs"
      provides: "Retired variant on LifecycleStatus, archive_retention_days on DiscoveryConfig"
      contains: "Retired"
    - path: "src/events/toml_writer.rs"
      provides: "Archive and cleanup helper functions"
      exports: ["collect_archivable_entries", "collect_expired_unapproved_ids", "remove_entries_by_id", "append_entries_to_archive_doc"]
    - path: "config/events.toml"
      provides: "archive_retention_days in [discovery] section"
      contains: "archive_retention_days"
  key_links:
    - from: "src/events/toml_writer.rs"
      to: "src/config/events.rs"
      via: "LifecycleStatus not directly used (raw TOML string comparison), but Direction and ExpiryConfidence imports remain"
      pattern: "status.*retired"
---

<objective>
Add Retired lifecycle status, archive_retention_days config field, and toml_writer helper functions for event archival and unapproved candidate cleanup.

Purpose: Provide the foundational types, config, and TOML document manipulation functions needed by Plan 02 to integrate archive-and-cleanup into the poll cycle.
Output: Extended LifecycleStatus enum, DiscoveryConfig with retention setting, and four toml_writer functions for archive/cleanup operations.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-lifecycle-management-and-integration/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Retired variant to LifecycleStatus and archive_retention_days to DiscoveryConfig</name>
  <files>src/config/events.rs, config/events.toml</files>
  <action>
1. In `src/config/events.rs`, add `Retired` variant to the `LifecycleStatus` enum after `Expired`:
   ```rust
   pub enum LifecycleStatus {
       Active,
       Expiring,
       Expired,
       Retired,
   }
   ```
   The `#[serde(rename_all = "lowercase")]` attribute already handles serialization to "retired".

2. Update the `Display` impl to add the `Retired` arm:
   ```rust
   LifecycleStatus::Retired => write!(f, "retired"),
   ```

3. Add `archive_retention_days` field to `DiscoveryConfig`:
   ```rust
   /// Number of days after expiry before an event is archived from events.toml
   /// to events_archive.toml. Default 30 days.
   #[serde(default = "default_archive_retention_days")]
   pub archive_retention_days: u32,
   ```

4. Add the default function:
   ```rust
   fn default_archive_retention_days() -> u32 {
       30
   }
   ```

5. In `config/events.toml`, add `archive_retention_days = 30` to the `[discovery]` section with a comment explaining the setting.

6. Run `cargo check` to ensure the new enum variant does not break any exhaustive matches. Per research, all existing uses are equality comparisons (`== LifecycleStatus::Active`, `!= LifecycleStatus::Active`, `== LifecycleStatus::Expired`) which are unaffected by the new variant.
  </action>
  <verify>
`cargo check` succeeds with no errors. `cargo test` passes all existing tests. `grep -r "Retired" src/config/events.rs` shows the variant and Display arm. `grep "archive_retention_days" src/config/events.rs` shows the field. `grep "archive_retention_days" config/events.toml` shows the config entry.
  </verify>
  <done>LifecycleStatus has four variants (Active, Expiring, Expired, Retired). DiscoveryConfig has archive_retention_days with default 30. All existing tests pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add archive and cleanup helper functions to toml_writer</name>
  <files>src/events/toml_writer.rs</files>
  <action>
Add four public functions to `src/events/toml_writer.rs` for archive and cleanup operations. These operate on `toml_edit::DocumentMut` values (same pattern as existing `append_candidates_to_doc` and `mark_expired_batch_in_doc`).

1. **`collect_archivable_entries`**: Scans `[[events]]` array and returns a `Vec<(String, toml_edit::Table)>` of entries to archive. An entry is archivable if:
   - `approved == true` (only archive operator-approved events, not auto-discovered candidates)
   - `status` is `"expired"` or `"retired"`
   - `expiry` date + `retention_days` < today (entry is older than retention period)
   Use defensive parsing: `unwrap_or(true)` for approved, `unwrap_or("active")` for status, skip entries where expiry cannot be parsed as `NaiveDate`. The returned tuple is `(id, cloned_table)`.

   ```rust
   pub fn collect_archivable_entries(
       doc: &DocumentMut,
       retention_days: u32,
       today: NaiveDate,
   ) -> Vec<(String, Table)> {
   ```

2. **`collect_expired_unapproved_ids`**: Scans `[[events]]` array and returns `Vec<String>` of event IDs for unapproved candidates past their expiry date. An entry matches if:
   - `approved == false`
   - `expiry` date < today (strict less-than, consistent with validation.rs pattern)
   Defensive parsing: `unwrap_or(true)` for approved (so manually-authored entries without explicit `approved` field are NOT removed), skip entries where expiry cannot be parsed.

   ```rust
   pub fn collect_expired_unapproved_ids(
       doc: &DocumentMut,
       today: NaiveDate,
   ) -> Vec<String> {
   ```

3. **`remove_entries_by_id`**: Removes entries from the `[[events]]` array by ID using `ArrayOfTables::retain()`. Returns `anyhow::Result<()>`.

   ```rust
   pub fn remove_entries_by_id(
       doc: &mut DocumentMut,
       ids_to_remove: &[String],
   ) -> anyhow::Result<()> {
   ```
   Implementation: get `as_array_of_tables_mut()`, call `retain(|table| { ... })` where the predicate keeps entries whose ID is NOT in `ids_to_remove`.

4. **`append_entries_to_archive_doc`**: Appends archived entries to an archive document. For each entry:
   - Set `status = "retired"` on the table
   - Add `archived_at = <RFC3339 timestamp>` field
   - Push to the `[[events]]` array of the archive document
   Create the `[[events]]` array if it does not exist in the archive doc.

   ```rust
   pub fn append_entries_to_archive_doc(
       archive_doc: &mut DocumentMut,
       entries: Vec<(String, Table)>,
       archived_at: &str,
   ) -> anyhow::Result<()> {
   ```

Add `use chrono::NaiveDate;` to the imports at the top of the file.

Add unit tests for each function:
- `test_collect_archivable_entries_filters_correctly`: Create a doc with 3 entries -- one expired+approved+old (archivable), one expired+approved+recent (not yet), one expired+unapproved (not archivable). Verify only the first is returned.
- `test_collect_expired_unapproved_ids`: Create a doc with entries -- one unapproved+past_expiry (matched), one unapproved+future_expiry (not matched), one approved+past_expiry (not matched). Verify only the first is returned.
- `test_remove_entries_by_id`: Create a doc with 3 entries, remove 1 by ID, verify 2 remain and the removed one is gone.
- `test_append_entries_to_archive_doc`: Create an empty archive doc, append 2 entries, verify both appear with `status = "retired"` and `archived_at` set.
  </action>
  <verify>
`cargo test -- toml_writer` runs all new and existing toml_writer tests. `cargo check` confirms no compilation errors. `grep -c "pub fn" src/events/toml_writer.rs` shows the new functions exist alongside existing ones.
  </verify>
  <done>Four new public functions in toml_writer.rs (collect_archivable_entries, collect_expired_unapproved_ids, remove_entries_by_id, append_entries_to_archive_doc) with unit tests for each. All existing toml_writer tests continue to pass.</done>
</task>

</tasks>

<verification>
1. `cargo check` -- zero compilation errors
2. `cargo test` -- all existing tests pass, new toml_writer tests pass
3. `grep "Retired" src/config/events.rs` -- variant exists
4. `grep "archive_retention_days" src/config/events.rs` -- config field exists
5. `grep "collect_archivable_entries\|collect_expired_unapproved_ids\|remove_entries_by_id\|append_entries_to_archive_doc" src/events/toml_writer.rs` -- all four functions exist
</verification>

<success_criteria>
- LifecycleStatus::Retired exists and serializes to "retired"
- DiscoveryConfig.archive_retention_days defaults to 30
- Four new toml_writer functions are tested and working
- All existing tests pass unchanged
- config/events.toml has archive_retention_days in [discovery] section
</success_criteria>

<output>
After completion, create `.planning/phases/21-lifecycle-management-and-integration/21-01-SUMMARY.md`
</output>
