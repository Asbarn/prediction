---
phase: 09-replay-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/health/mod.rs
  - src/config/system.rs
  - src/feed/pipeline.rs
  - src/events/registry.rs
  - src/spread/patterns.rs
  - src/paper_trade/tracker.rs
  - src/lib.rs
  - src/main.rs
  - tests/schema_golden_test.rs
autonomous: true
requirements: [OBSV-05, OBSV-06]

must_haves:
  truths:
    - "HTTP GET /health returns JSON with per-feed connection status, last update time, active event count, and uptime"
    - "Health endpoint runs on a separate configurable port (default 9001) without conflicting with Prometheus (port 9000)"
    - "All four JSONL output types (RecordLine, SpreadResult, ArbSignal, TradeEvent) have serde roundtrip golden tests that fail on schema changes"
    - "SpreadResult and TradeEvent gain Deserialize derives for offline analysis tooling"
    - "JSONL schema documentation exists as a schema spec file"
  artifacts:
    - path: "src/health/mod.rs"
      provides: "HealthState, HealthResponse, FeedStatus, axum handler, start_health_server"
      exports: ["HealthState", "start_health_server"]
    - path: "src/config/system.rs"
      provides: "HealthConfig with port and enabled fields"
      contains: "HealthConfig"
    - path: "src/events/registry.rs"
      provides: "event_count() method on EventRegistry"
      contains: "fn event_count"
    - path: "tests/schema_golden_test.rs"
      provides: "Golden serde roundtrip tests for all 4 JSONL types"
      min_lines: 80
  key_links:
    - from: "src/health/mod.rs"
      to: "src/feed/health.rs"
      via: "Arc<VenueHealth> references in HealthState"
      pattern: "VenueHealth"
    - from: "src/health/mod.rs"
      to: "src/events/registry.rs"
      via: "Arc<RwLock<EventRegistry>> for event_count"
      pattern: "event_count"
    - from: "src/main.rs"
      to: "src/health/mod.rs"
      via: "tokio::spawn(start_health_server)"
      pattern: "start_health_server"
    - from: "src/feed/pipeline.rs"
      to: "src/main.rs"
      via: "PipelineHandles returning VenueHealth references"
      pattern: "PipelineHandles"
---

<objective>
Add an HTTP `/health` endpoint reporting per-feed connection status and system uptime, stabilize all JSONL schemas with Deserialize derives and golden roundtrip tests, and add an `event_count()` method to EventRegistry.

Purpose: Provides operational monitoring capability (OBSV-05) and ensures JSONL output is stable for offline Python/Jupyter analysis tooling (OBSV-06).
Output: `src/health/mod.rs`, updated config, golden schema tests, schema documentation
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-replay-and-hardening/09-RESEARCH.md

@src/feed/health.rs
@src/feed/pipeline.rs
@src/events/registry.rs
@src/config/system.rs
@src/main.rs
@src/spread/patterns.rs
@src/paper_trade/tracker.rs
@src/feed/traits.rs
@src/signal/types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Health endpoint module, config, and pipeline refactor to expose VenueHealth</name>
  <files>
    Cargo.toml
    src/health/mod.rs
    src/config/system.rs
    src/feed/pipeline.rs
    src/events/registry.rs
    src/lib.rs
    src/main.rs
  </files>
  <action>
1. **Add axum dependency**: `cargo add axum@0.8 --no-default-features --features json,tokio`

2. **Create `src/health/mod.rs`**:
   - `HealthState` struct (Clone) holding `Vec<Arc<VenueHealth>>`, `Arc<RwLock<EventRegistry>>`, `started_at: DateTime<Utc>`
   - `HealthResponse` struct (Serialize) with `status: String` ("ok" if any feed connected, "degraded" if none), `uptime_secs: i64`, `feeds: Vec<FeedStatus>`, `active_event_count: usize`
   - `FeedStatus` struct (Serialize) with `venue: String`, `connected: bool`, `last_message_at: Option<DateTime<Utc>>`, `last_error: Option<String>`, `connection_count: u64`
   - `async fn health_handler(State(state): State<HealthState>) -> Json<HealthResponse>` -- reads from VenueHealth atomics/mutexes (lightweight, non-blocking on pipeline channels), reads event_count from registry
   - `pub async fn start_health_server(state: HealthState, port: u16)` -- creates `Router::new().route("/health", get(health_handler)).with_state(state)`, binds to `0.0.0.0:{port}`, calls `axum::serve(listener, app).await`
   - Add 2 unit tests: (a) health_handler returns correct structure with mock data, (b) status is "degraded" when no feeds are connected

3. **Add `HealthConfig` to `src/config/system.rs`**:
   - `HealthConfig { pub port: u16, pub enabled: bool }` with `#[serde(default)]`, Default impl returning `port: 9001, enabled: true`
   - Add `#[serde(default)] pub health: HealthConfig` field to `SystemConfig`

4. **Add `pub fn event_count(&self) -> usize` to `EventRegistry` in `src/events/registry.rs`**:
   - Return the number of registered event mappings (length of the internal mappings vec)

5. **Refactor `run_multi_venue_pipeline()` in `src/feed/pipeline.rs`**:
   - Create a `PipelineHandles` struct: `pub struct PipelineHandles { pub snapshot_rx: mpsc::Receiver<MarketSnapshot>, pub venue_health: Vec<Arc<VenueHealth>> }`
   - Change return type of `run_multi_venue_pipeline()` from `anyhow::Result<mpsc::Receiver<MarketSnapshot>>` to `anyhow::Result<PipelineHandles>`
   - In `run_live_multi_venue()`, create `VenueHealth::new(venue)` for each venue BEFORE the pipeline section and collect them into a Vec. Pass each `Arc<VenueHealth>` to its respective supervisor (the supervisors already accept and use VenueHealth -- check if they do, if not, call `health.mark_available()` after successful processor spawn). Return `PipelineHandles` with the collected health handles.
   - NOTE: Check whether `run_live_multi_venue` already creates VenueHealth internally. Research open question #1 indicates VenueHealth is created inside `run_live_multi_venue` but not returned. The fix is to return them as part of PipelineHandles.
   - For Mock/Replay modes, return empty `venue_health` vec in PipelineHandles.

6. **Register health module in `src/lib.rs`**: Add `pub mod health;`

7. **Wire health endpoint in `src/main.rs`**:
   - Extract `snapshot_rx` and `venue_health` from the PipelineHandles returned by `run_multi_venue_pipeline()`
   - Construct `HealthState { venue_health, event_registry: event_registry.clone(), started_at: Utc::now() }`
   - If `config.system.health.enabled`, spawn `start_health_server(health_state, config.system.health.port)` via `tokio::spawn`
  </action>
  <verify>
    `cargo build` succeeds. `cargo test` passes. Manually verify with `cargo run -- --mock` then `curl http://localhost:9001/health` returns JSON with status, uptime_secs, feeds, and active_event_count fields.
  </verify>
  <done>
    HTTP GET /health returns JSON HealthResponse with per-feed status, last_message_at, active_event_count, and uptime. Health endpoint runs on port 9001 (configurable). EventRegistry has event_count() method. Pipeline returns PipelineHandles with VenueHealth references.
  </done>
</task>

<task type="auto">
  <name>Task 2: JSONL schema stabilization with Deserialize derives, schema version, and golden tests</name>
  <files>
    src/spread/patterns.rs
    src/paper_trade/tracker.rs
    tests/schema_golden_test.rs
  </files>
  <action>
1. **Add `Deserialize` to `SpreadResult` and supporting types in `src/spread/patterns.rs`**:
   - Add `serde::Deserialize` to the derive list for `SpreadResult`, `SpreadPattern`, and `GrossSpread`
   - `ThresholdComponents` already has Deserialize (confirmed from codebase grep)
   - All Decimal fields in SpreadResult already use `#[serde(with = "rust_decimal::serde::str")]` -- verify Deserialize compatibility with this serde helper (it supports both)

2. **Add `Deserialize` to `TradeEvent` and supporting types in `src/paper_trade/tracker.rs`**:
   - Add `serde::Deserialize` to the derive list for the `TradeEvent` enum
   - Ensure all nested types within TradeEvent variants also derive Deserialize (PaperPosition, PositionStatus, etc.)
   - NOTE: TradeEvent is `pub(crate)` or private -- make it `pub` if it's private, since offline tooling needs to be able to import the type. If it uses `#[serde(tag = "event_type")]` or similar, verify the tagged enum serialization round-trips correctly.

3. **Create `tests/schema_golden_test.rs`** with golden serde roundtrip tests for all 4 JSONL types:

   a. **RecordLine roundtrip test**: Create a RecordLine with known field values, serialize to JSON string, deserialize back, assert all fields match. Also assert that specific expected JSON field names are present in the serialized output (field name stability check).

   b. **SpreadResult roundtrip test**: Create a SpreadResult with known values for all fields (use `rust_decimal_macros::dec!` for Decimal literals). Serialize to `serde_json::Value`, verify all expected field names are present (list every field explicitly: event_id, pattern, gross_spread, net_spread, buy_fill_price, sell_fill_price, buy_fee, sell_fee, carry_cost, total_cost, buy_fill_ratio, sell_fill_ratio, target_notional, timestamp_ms, poly_exchange_ts, kalshi_exchange_ts, threshold, threshold_components). Verify Decimal fields serialize as strings (not JSON numbers). Deserialize back and assert key fields match.

   c. **ArbSignal roundtrip test**: Create an ArbSignal with known values. Serialize to JSON string, deserialize back, assert key fields match. ArbSignal already has both Serialize + Deserialize. Verify all expected top-level fields are present.

   d. **TradeEvent roundtrip test**: Create each TradeEvent variant, serialize, verify JSON field names, deserialize back.

   Each test MUST:
   - Use `serde_json::to_value` to check field presence
   - Use `serde_json::to_string` -> `serde_json::from_str` for roundtrip
   - Assert specific field names exist (schema stability)
   - Assert Decimal fields are strings in JSON (precision stability)

4. **Schema documentation**: Add a doc comment block at the top of each JSONL-producing type (RecordLine, SpreadResult, ArbSignal, TradeEvent) with `/// ## JSONL Schema (v1.0)` documenting each field's name, JSON type, and description. This serves as inline schema documentation without requiring a separate file.
  </action>
  <verify>
    `cargo test schema_golden` runs all 4+ golden tests and passes. `cargo test` passes with no regressions. Verify that `serde_json::from_str::<SpreadResult>(json_str)` works (Deserialize added correctly).
  </verify>
  <done>
    SpreadResult and TradeEvent have Deserialize derives. All 4 JSONL types have golden roundtrip tests that will fail on any field addition/removal/rename. Schema documentation exists as doc comments on each type.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` -- project compiles with axum 0.8 dependency
2. `cargo test` -- all existing tests pass + new health and schema golden tests pass
3. `cargo run -- --mock` + `curl http://localhost:9001/health` -- returns valid JSON with status, uptime_secs, feeds array, and active_event_count
4. Health endpoint on port 9001 does NOT conflict with Prometheus on port 9000
5. All 4 JSONL type roundtrip tests pass (RecordLine, SpreadResult, ArbSignal, TradeEvent)
6. SpreadResult and TradeEvent can be deserialized from JSON (Deserialize derive added)
</verification>

<success_criteria>
- HTTP /health endpoint serves JSON on configurable port
- Health response includes per-feed connected status, last_message_at, connection_count, last_error, active_event_count, and uptime_secs
- EventRegistry has event_count() method
- PipelineHandles exposes VenueHealth references to main.rs
- SpreadResult + TradeEvent have Deserialize derives
- Golden roundtrip tests exist for RecordLine, SpreadResult, ArbSignal, TradeEvent
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-replay-and-hardening/09-01-SUMMARY.md`
</output>
