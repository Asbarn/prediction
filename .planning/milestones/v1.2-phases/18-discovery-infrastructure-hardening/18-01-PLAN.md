---
phase: 18-discovery-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/events.rs
  - src/events/toml_writer.rs
autonomous: true
requirements:
  - LIFE-04
  - INTG-03

must_haves:
  truths:
    - "DiscoveryConfig has consecutive_absence_threshold field (default 3) and partial_response_threshold field (default 0.2)"
    - "toml_writer exposes batch mutation functions that operate on DocumentMut in-place without file I/O"
    - "Batch functions support both candidate appends and expired-status marks on a single DocumentMut"
  artifacts:
    - path: "src/config/events.rs"
      provides: "DiscoveryConfig with consecutive_absence_threshold and partial_response_threshold fields"
      contains: "consecutive_absence_threshold"
    - path: "src/events/toml_writer.rs"
      provides: "Batch TOML mutation functions (append_candidates_to_doc, mark_expired_batch_in_doc)"
      exports: ["append_candidates_to_doc", "mark_expired_batch_in_doc"]
  key_links:
    - from: "src/events/toml_writer.rs"
      to: "toml_edit::DocumentMut"
      via: "in-place mutation without file I/O"
      pattern: "fn.*doc.*&mut DocumentMut"
---

<objective>
Add foundation types for Phase 18 hardening: extend DiscoveryConfig with consecutive-absence and partial-response thresholds, and add batch TOML mutation functions that operate on a single DocumentMut in-place (no file I/O per mutation).

Purpose: Provide the config knobs and batch-write primitives that Plan 02 will wire into the poll cycle. Separating these from the lifecycle integration keeps each plan focused and testable.

Output: Extended DiscoveryConfig struct, batch append/expire functions in toml_writer.rs.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-discovery-infrastructure-hardening/18-RESEARCH.md
@src/config/events.rs
@src/events/toml_writer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DiscoveryConfig with absence and partial-response thresholds</name>
  <files>src/config/events.rs</files>
  <action>
Add two new fields to `DiscoveryConfig`:

1. `consecutive_absence_threshold: u32` -- number of consecutive polls an instrument must be absent before expiry. Default 3. Use `#[serde(default = "default_consecutive_absence_threshold")]` with a free function `fn default_consecutive_absence_threshold() -> u32 { 3 }`.

2. `partial_response_threshold: f64` -- fractional drop threshold (0.0-1.0) that triggers suspect-response detection. Default 0.2 (20%). Use `#[serde(default = "default_partial_response_threshold")]` with a free function `fn default_partial_response_threshold() -> f64 { 0.2 }`.

Both fields are `pub`. Update the `Default` impl for `DiscoveryConfig` to include these new fields with their defaults. Existing fields remain unchanged.

Note: Do NOT change any other struct in this file. The existing `DiscoveryConfig` methods (`min_poll_interval_secs`) are unaffected.
  </action>
  <verify>Run `cargo check` -- no compilation errors. Grep for `consecutive_absence_threshold` in `src/config/events.rs` to confirm presence.</verify>
  <done>DiscoveryConfig has both new fields with serde defaults, Default impl includes them, cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add batch TOML mutation functions to toml_writer</name>
  <files>src/events/toml_writer.rs</files>
  <action>
Add two new public functions alongside the existing `append_candidate_to_toml` and `mark_expired_in_toml`:

1. `pub fn append_candidates_to_doc(doc: &mut DocumentMut, candidates: &[CandidateMapping]) -> anyhow::Result<()>`
   - Operates on a `DocumentMut` in-place (no file read/write).
   - Gets `doc["events"]` as `array_of_tables_mut()`. For each candidate in the slice, build a `Table` with the same fields as the existing `append_candidate_to_toml` (id, asset, strike, direction, expiry, approved=false, status="active", plus venue sub-tables for deribit/kalshi/polymarket). Push each table to the array.
   - This reuses the same field-population logic as the existing single-candidate function. Factor out a private helper `fn build_candidate_table(candidate: &CandidateMapping) -> Table` that both the old function and this new one can call, to avoid duplication.

2. `pub fn mark_expired_batch_in_doc(doc: &mut DocumentMut, event_ids: &[String]) -> anyhow::Result<()>`
   - Operates on a `DocumentMut` in-place (no file read/write).
   - Gets `doc["events"]` as `array_of_tables_mut()`. For each event_id in the slice, iterate the array to find the matching `id` field and set `status = "expired"`.
   - Use `break` after finding each match (IDs are unique).

Both functions return `Ok(())` on success, `Err` if `[[events]]` array is missing.

Do NOT remove the existing `append_candidate_to_toml` or `mark_expired_in_toml` functions -- they are still used by other code paths and will be refactored in Plan 02.
  </action>
  <verify>Run `cargo check` -- no compilation errors. Grep for `append_candidates_to_doc` and `mark_expired_batch_in_doc` in `src/events/toml_writer.rs` to confirm both exist as pub functions.</verify>
  <done>Both batch functions compile, operate on DocumentMut without file I/O, and the existing single-mutation functions still compile and work.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes with no errors
2. `cargo test` passes (existing tests unbroken)
3. DiscoveryConfig round-trips through serde with the new fields defaulting correctly (existing TOML configs without these fields still deserialize)
4. Both batch functions are public and importable from `crate::events::toml_writer`
</verification>

<success_criteria>
- DiscoveryConfig has consecutive_absence_threshold (default 3) and partial_response_threshold (default 0.2)
- toml_writer has append_candidates_to_doc and mark_expired_batch_in_doc that mutate DocumentMut in-place
- All existing code compiles without changes (backward compatible)
- cargo test passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-discovery-infrastructure-hardening/18-01-SUMMARY.md`
</output>
