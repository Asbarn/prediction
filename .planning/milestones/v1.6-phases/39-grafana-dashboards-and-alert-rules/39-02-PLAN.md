---
phase: 39-grafana-dashboards-and-alert-rules
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - infra/cdk/lib/prediction-stack.ts
autonomous: false
requirements: [MON-04, MON-05, MON-06, MON-07, MON-08]

must_haves:
  truths:
    - "Feed Health dashboard loads in Grafana and shows feed_available per venue with live data"
    - "Signal Quality dashboard loads in Grafana and shows arb signal metrics with live data"
    - "Paper Trade P&L dashboard loads in Grafana and shows daily P&L and trade counts"
    - "System Health dashboard loads in Grafana and shows active expiries, subscriptions, and lifecycle metrics"
    - "Alert rules appear in Grafana Alerting with correct evaluation states"
    - "All dashboards survive Grafana container restart (provisioned from files, not just UI)"
  artifacts:
    - path: "infra/cdk/lib/prediction-stack.ts"
      provides: "CDK user-data writes all provisioning files to EC2"
      contains: "dashboards/json"
  key_links:
    - from: "infra/cdk/lib/prediction-stack.ts"
      to: "grafana/provisioning/"
      via: "user-data heredoc writes provisioning files to /opt/prediction/grafana/provisioning/"
      pattern: "mkdir -p.*dashboards/json"
    - from: "docker-compose grafana volume mount"
      to: "/opt/prediction/grafana/provisioning"
      via: "volume mount at /etc/grafana/provisioning:ro"
      pattern: "grafana/provisioning:/etc/grafana/provisioning"
---

<objective>
Integrate all Grafana provisioning files into CDK user-data, deploy to production, and verify dashboards and alerts work with live metrics.

Purpose: Make dashboards and alert rules automatically deploy with infrastructure, ensuring they persist across EC2 instance replacements and are version-controlled in CDK.

Output: Updated CDK stack deployed to AWS with all 4 dashboards and 3 alert rules visible and operational in Grafana.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-grafana-dashboards-and-alert-rules/39-RESEARCH.md
@.planning/phases/39-grafana-dashboards-and-alert-rules/39-01-SUMMARY.md
@infra/cdk/lib/prediction-stack.ts
@grafana/provisioning/dashboards/provider.yml
@grafana/provisioning/dashboards/json/feed-health.json
@grafana/provisioning/dashboards/json/signal-quality.json
@grafana/provisioning/dashboards/json/paper-trade-pnl.json
@grafana/provisioning/dashboards/json/system-health.json
@grafana/provisioning/alerting/rules.yml
@grafana/provisioning/alerting/contact-points.yml
@grafana/provisioning/alerting/notification-policies.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CDK user-data to write all Grafana provisioning files and deploy</name>
  <files>infra/cdk/lib/prediction-stack.ts</files>
  <action>
Update the user-data section in `prediction-stack.ts` to write all provisioning files created in Plan 01. The existing pattern writes `amp.yml` via heredoc -- extend this to write all new files.

**Modifications to the existing Grafana provisioning section (around line 289-307):**

1. **Update the `mkdir -p` command** to create all required subdirectories:
   ```
   mkdir -p /opt/prediction/grafana/provisioning/{datasources,dashboards/json,alerting}
   ```

2. **Update the amp.yml heredoc** to include `uid: amp` (add the line after `- name: AMP`).

3. **Add dashboard provider YAML** -- Write `grafana/provisioning/dashboards/provider.yml` content via heredoc. Place after the amp.yml write block.

4. **Add all 4 dashboard JSON files** -- Write each dashboard JSON file content via heredoc to `/opt/prediction/grafana/provisioning/dashboards/json/`. Since JSON contains special characters, use a quoted heredoc delimiter (e.g., `'DASHEOF'`) to prevent shell expansion.

   IMPORTANT: The dashboard JSON files are large. Rather than embedding the full JSON in the CDK TypeScript string array (which would be hundreds of lines per dashboard), use a pragmatic approach:
   - Read each JSON file from `grafana/provisioning/dashboards/json/` at the time of writing the CDK user-data
   - Write the JSON content as a single heredoc block per dashboard
   - Each line of JSON becomes one string in the addCommands array

5. **Add alerting YAML files** -- Write `rules.yml`, `contact-points.yml`, and `notification-policies.yml` via heredocs to `/opt/prediction/grafana/provisioning/alerting/`.

6. **All provisioning file writes MUST come BEFORE the docker-compose.yml heredoc** (which starts around line 309). This ensures files exist when Grafana container starts.

After updating CDK:
- Run `cd infra/cdk && npx cdk diff` to verify changes
- Run `cd infra/cdk && npx cdk deploy --require-approval never` to deploy
- After deploy, restart services on EC2 via SSM to pick up new provisioning files:
  ```
  aws ssm send-command --instance-ids <INSTANCE_ID> --document-name "AWS-RunShellScript" --parameters 'commands=["cd /opt/prediction && docker compose down && docker compose pull && docker compose up -d"]'
  ```
  Get instance ID from `aws ec2 describe-instances --filters "Name=tag:Name,Values=*prediction*" --query 'Reservations[].Instances[].InstanceId' --output text`
  </action>
  <verify>
1. `cd infra/cdk && npx cdk diff` shows user-data changes (new provisioning file writes)
2. `npx cdk deploy` succeeds
3. After SSM restart, verify Grafana is accessible at http://<EC2-IP>:3000
4. Verify dashboards exist: `curl -s -u admin:admin http://<EC2-IP>:3000/api/search?type=dash-db` returns 4 dashboards
5. Verify alert rules exist: `curl -s -u admin:admin http://<EC2-IP>:3000/api/v1/provisioning/alert-rules` returns 3 rules
  </verify>
  <done>
CDK stack deployed with all provisioning files embedded in user-data. Grafana container starts with 4 dashboards and 3 alert rules automatically provisioned.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dashboards and alerts in Grafana UI</name>
  <action>
Human verification of deployed Grafana dashboards and alert rules.

What was built: Four operational dashboards (Feed Health, Signal Quality, Paper Trade P&L, System Health) and three alert rules (Feed Down, Zero Spread Computations, High Staleness Rate) deployed to production Grafana.

Steps to verify:
1. Open Grafana at http://<EC2-IP>:3000 (admin/admin)
2. Navigate to Dashboards -- verify all 4 dashboards appear:
   - Feed Health: Check feed_available shows UP/DOWN per venue, reconnection rate and latency time series have data
   - Signal Quality: Check arb signals and staleness rejection panels have data
   - Paper Trade P&L: Check daily P&L and trade count panels render (may show 0 if no trades active)
   - System Health: Check active expiries, subscriptions, and lifecycle poll panels have data
3. Navigate to Alerting > Alert rules -- verify 3 rules appear:
   - "Feed Down" (critical, 5m for duration)
   - "Zero Spread Computations" (warning, 30m for duration)
   - "High Staleness Rejection Rate" (warning, 5m for duration)
4. Check alert rule states -- they should show "Normal" or "Pending" (not "Error")
5. Verify at least one dashboard shows live-updating data (auto-refresh 30s)
  </action>
  <verify>User confirms all dashboards render with data and alert rules evaluate without errors</verify>
  <done>All 4 dashboards and 3 alert rules verified operational in Grafana UI</done>
</task>

</tasks>

<verification>
1. CDK stack deployed successfully with updated user-data
2. All 4 dashboards visible and loading data in Grafana UI
3. All 3 alert rules visible with correct evaluation states (not Error)
4. Dashboards auto-refresh and show live metrics
5. Provisioning survives container restart (stop/start Grafana container, dashboards still present)
</verification>

<success_criteria>
- Feed Health dashboard shows per-venue feed availability, reconnection rate, and message latency with live data
- Signal Quality dashboard shows arb signals, staleness rejections, and computation rate
- Paper Trade P&L dashboard shows daily P&L, win rate, and trade counts
- System Health dashboard shows active expiries, subscriptions, lifecycle polls, and proposals
- Three alert rules evaluate without errors and would fire on their respective conditions
- All provisioning is infrastructure-as-code via CDK user-data (survives instance replacement)
</success_criteria>

<output>
After completion, create `.planning/phases/39-grafana-dashboards-and-alert-rules/39-02-SUMMARY.md`
</output>
