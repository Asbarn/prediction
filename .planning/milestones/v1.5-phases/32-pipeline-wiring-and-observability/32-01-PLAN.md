---
phase: 32-pipeline-wiring-and-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/subscription/manager.rs
autonomous: true
requirements:
  - PIPE-03

must_haves:
  truths:
    - "SubscriptionManager tracks Derive instruments with HashSet diff reconciliation"
    - "Watch channel pushes sorted Derive instrument updates to supervisor"
    - "CleanupEvent.derive_instruments is populated from actual diff (not Vec::new())"
    - "Subscription metrics emit with venue=derive label"
  artifacts:
    - path: "src/subscription/manager.rs"
      provides: "Derive venue support in SubscriptionManager"
      contains: "current_derive"
  key_links:
    - from: "src/subscription/manager.rs"
      to: "SubscriptionSenders/SubscriptionReceivers"
      via: "derive field on both structs"
      pattern: "derive.*watch::Sender"
    - from: "src/subscription/manager.rs reconcile()"
      to: "CleanupEvent"
      via: "derive_instruments populated from removed set"
      pattern: "derive_instruments.*removed"
---

<objective>
Extend SubscriptionManager with Derive venue support following the existing 3-venue pattern.

Purpose: SubscriptionManager must track Derive instruments so the pipeline can dynamically add/remove Derive subscriptions via watch channels, matching the Deribit/Polymarket/Kalshi pattern exactly.
Output: SubscriptionManager with 4-venue support, Derive watch channels, and populated CleanupEvent.derive_instruments.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-pipeline-wiring-and-observability/32-RESEARCH.md
@src/subscription/manager.rs
@src/config/events.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Derive fields to SubscriptionSenders, SubscriptionReceivers, and SubscriptionManager</name>
  <files>src/subscription/manager.rs</files>
  <action>
Add Derive support to SubscriptionManager by extending all existing 3-venue patterns to 4-venue:

1. Add `derive: watch::Sender<Vec<String>>` to `SubscriptionSenders` struct
2. Add `derive: watch::Receiver<Vec<String>>` to `SubscriptionReceivers` struct
3. Add `derive_tx: watch::Sender<Vec<String>>` field to `SubscriptionManager` struct
4. Add `current_derive: HashSet<String>` field to `SubscriptionManager` struct
5. Update `compute_desired_instruments()`:
   - Change return type from 3-tuple to 4-tuple adding `HashSet<String>` for Derive
   - Add `let mut derive = HashSet::new();`
   - In the active_approved loop, add: `if let Some(ref dr) = mapping.venues.derive { derive.insert(dr.instrument.clone()); }`
   - Return the 4-tuple
6. Update ALL call sites of `compute_desired_instruments()` to destructure the 4-tuple (both `reconcile()` and `create_channels()`)
7. Update `create_channels()`:
   - Create `(derive_tx, derive_rx) = watch::channel(initial_derive)` with sorted initial instruments
   - Return Derive fields in both SubscriptionSenders and SubscriptionReceivers
8. Update constructor (`new()`) to store `derive_tx` and initialize `current_derive: HashSet::new()`
  </action>
  <verify>Run `cargo check` -- must compile with zero errors. Run `cargo test -p prediction -- subscription` -- all existing subscription tests pass.</verify>
  <done>SubscriptionSenders/Receivers have derive fields, compute_desired_instruments returns 4-tuple, create_channels seeds Derive watch channel with initial instruments.</done>
</task>

<task type="auto">
  <name>Task 2: Add Derive diff/reconcile block and populate CleanupEvent.derive_instruments</name>
  <files>src/subscription/manager.rs</files>
  <action>
Add Derive reconciliation in `reconcile()` and populate CleanupEvent:

1. In `reconcile()`, add a Derive diff/log/send/metrics block identical to Deribit's:
   - Compute `added` and `removed` HashSet diffs against `self.current_derive`
   - Log additions/removals with `tracing::info!` using `venue = "derive"` field
   - Sort desired instruments into a Vec and call `self.derive_tx.send_replace(sorted_vec)`
   - Emit `metrics::gauge!("subscription_active", "venue" => "derive")` with current count
   - Emit `metrics::counter!("subscription_activations_total", "venue" => "derive")` for added count
   - Emit `metrics::counter!("subscription_removals_total", "venue" => "derive")` for removed count
   - Update `self.current_derive = desired_derive;`

2. Update `CleanupEvent` construction in `reconcile()`:
   - Replace `derive_instruments: Vec::new()` with `derive_instruments: removed_derive.into_iter().collect()`
   - Where `removed_derive` is the set of instruments removed during Derive diff (same pattern as Deribit/Kalshi cleanup)

Follow the exact same pattern as the Deribit block in reconcile(). Use identical logging format, metric names with venue label, and diff computation approach.
  </action>
  <verify>Run `cargo check` -- compiles. Run `cargo test -p prediction -- subscription` -- all tests pass. Grep for `derive_instruments: Vec::new()` in manager.rs -- should no longer exist (replaced with actual diff population).</verify>
  <done>Derive reconciliation diff works with HashSet add/remove, subscription metrics emit with venue=derive, CleanupEvent.derive_instruments populated from actual removed instruments.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes with all Derive fields added
2. `cargo test -p prediction` passes (all 642+ tests)
3. `SubscriptionSenders` and `SubscriptionReceivers` both have `derive` field
4. `compute_desired_instruments` returns 4-tuple
5. `reconcile()` has Derive diff block with metrics
6. `CleanupEvent.derive_instruments` is populated from diff, not hardcoded empty
</verification>

<success_criteria>
SubscriptionManager manages Derive instruments with the same HashSet diff, watch channel push, Notify ordering, and metrics emission as Deribit/Polymarket/Kalshi. CleanupEvent carries actual removed Derive instruments for processor state eviction.
</success_criteria>

<output>
After completion, create `.planning/phases/32-pipeline-wiring-and-observability/32-01-SUMMARY.md`
</output>
