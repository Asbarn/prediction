---
phase: 32-pipeline-wiring-and-observability
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - src/feed/pipeline.rs
  - src/main.rs
  - src/feed/health.rs
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05

must_haves:
  truths:
    - "Derive snapshots flow through run_live_multi_venue and reach SpreadEngine/SignalEngine/PaperTradeTracker"
    - "DeriveSupervisor receives dynamic instrument updates via watch channel from SubscriptionManager"
    - "Prometheus metrics expose Derive connection status, message rate, subscription count, and reconnection events"
    - "Derive pipeline has crash isolation via child CancellationToken"
  artifacts:
    - path: "src/feed/pipeline.rs"
      provides: "Derive pipeline block in run_live_multi_venue"
      contains: "DeriveSupervisor"
    - path: "src/main.rs"
      provides: "Derive venue availability log and SubscriptionReceivers destructuring"
      contains: "derive"
    - path: "src/feed/health.rs"
      provides: "Reconnection counter metric for all venues"
      contains: "feed_reconnections_total"
  key_links:
    - from: "src/feed/pipeline.rs"
      to: "snapshot_tx"
      via: "forward_snapshots cloned sender"
      pattern: "forward_snapshots.*Venue::Derive"
    - from: "src/feed/pipeline.rs"
      to: "DeriveSupervisor"
      via: "tokio::spawn(supervisor.run(supervisor_tx))"
      pattern: "DeriveSupervisor::new"
    - from: "src/feed/pipeline.rs"
      to: "DeriveProcessor"
      via: "tokio::spawn(processor.run())"
      pattern: "DeriveProcessor::new"
    - from: "src/main.rs"
      to: "SubscriptionReceivers"
      via: "derive_rx destructuring"
      pattern: "derive.*watch::Receiver"
---

<objective>
Wire Derive feed into run_live_multi_venue pipeline and add reconnection metrics.

Purpose: Derive MarketSnapshots must flow through the shared fan-in channel to reach SpreadEngine, SignalEngine, and PaperTradeTracker -- completing the 4th venue integration. Reconnection metrics close PIPE-05.
Output: Derive pipeline block in run_live_multi_venue, venue availability logging, reconnection counter metric.
</objective>

<execution_context>
@C:/Users/Think/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Think/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-pipeline-wiring-and-observability/32-RESEARCH.md
@.planning/phases/32-pipeline-wiring-and-observability/32-01-SUMMARY.md
@src/feed/pipeline.rs
@src/main.rs
@src/feed/health.rs
@src/feed/derive/supervisor.rs
@src/feed/derive/normalize.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Derive pipeline block to run_live_multi_venue and update main.rs</name>
  <files>src/feed/pipeline.rs, src/main.rs</files>
  <action>
Wire Derive into the live pipeline following the existing Deribit pipeline block pattern exactly:

**In src/feed/pipeline.rs:**

1. Add `use` imports for `DeriveProcessor` (from `crate::feed::derive::normalize`) and `DeriveSupervisor` (from `crate::feed::derive::supervisor`)
2. Add `derive_cleanup_tx/derive_cleanup_rx` channel pair (mpsc or broadcast, matching existing cleanup channel pattern) alongside existing cleanup channels
3. Destructure `derive_rx` from `SubscriptionReceivers` option -- when `subscription_rx` is `Some(...)`, destructure the 4th field. When `None`, create a one-shot watch channel seeded from `config.derive.instruments`
4. Add `// --- Derive pipeline ---` block BEFORE `drop(snapshot_tx)`. Follow Deribit's 7-step pattern:
   - Create `VenueHealth::new(Venue::Derive)` and push to `venue_health_handles`
   - Create child `CancellationToken` for crash isolation
   - Start `RecordingService::start(recording_dir.join("derive"), Venue::Derive, venue_cancel.clone())`
   - Create `VenueRateLimiter::new("derive", config.derive.rate_limit_per_second)` and insert into `rate_limiters`
   - Create mpsc channel for supervisor raw messages
   - Create `DeriveSupervisor::new(config.derive.clone(), instruments_rx, venue_cancel.clone(), rate_limiter, health.clone())` -- NOTE: DeriveSupervisor takes `Option<VenueRateLimiter>` as param, wrap with `Some()`
   - Spawn `supervisor.run(supervisor_tx)`
   - Create `DeriveProcessor::new(supervisor_rx, Some(derive_recording.sender()), venue_cancel.clone(), &config.derive, derive_cleanup_rx)` and spawn `processor.run()`
   - Clone `snapshot_tx` and spawn `forward_snapshots(venue_snapshot_rx, fan_in_tx, Venue::Derive, venue_cancel, Some(health.clone()), event_registry.clone())`
5. Add `derive_cleanup_tx` to the `PipelineHandles.cleanup_txs` vector (or however cleanup senders are collected for the SubscriptionManager)

CRITICAL: Place the Derive block BEFORE `drop(snapshot_tx)` -- otherwise forward_snapshots gets a dead sender.
CRITICAL: DeriveProcessor::new takes `&config.derive` (not staleness_threshold_ms like DeribitProcessor).
CRITICAL: No auth/credential guard needed for Derive (unlike Kalshi).

**In src/main.rs:**

1. Update venue availability log to include `derive = "available (public, no auth)"`
2. Update `SubscriptionReceivers` destructuring to extract `derive_rx` field (if SubscriptionReceivers is destructured in main.rs rather than pipeline.rs, update accordingly)
  </action>
  <verify>Run `cargo check` -- must compile with zero errors. Run `cargo test -p prediction` -- all existing tests pass. Grep for `DeriveSupervisor` in pipeline.rs to confirm the block exists. Grep for `drop(snapshot_tx)` in pipeline.rs and confirm Derive block appears before it.</verify>
  <done>Derive pipeline block spawns DeriveSupervisor, DeriveProcessor, RecordingService, and forward_snapshots. Derive snapshots flow to shared fan-in channel. main.rs logs Derive availability.</done>
</task>

<task type="auto">
  <name>Task 2: Add feed_reconnections_total counter metric in VenueHealth</name>
  <files>src/feed/health.rs</files>
  <action>
Add a Prometheus counter for reconnection events in VenueHealth::increment_connections():

1. In `increment_connections()` method, add: `metrics::counter!("feed_reconnections_total", "venue" => self.venue_label()).increment(1);`
   - This benefits ALL venues (Deribit, Polymarket, Kalshi, Derive) not just Derive
   - The venue label comes from the existing venue label method on VenueHealth (check how other metrics derive the venue string -- likely `self.venue.as_str()` or a stored label)

2. Verify the venue label method exists. If VenueHealth stores a `Venue` enum, use its Display/as_str impl. If it stores a string label, use that.

This is a one-line metric addition. Do NOT create Derive-specific metric names -- use the existing venue label pattern.
  </action>
  <verify>Run `cargo check` -- compiles. Run `cargo test -p prediction` -- all tests pass. Grep for `feed_reconnections_total` in health.rs to confirm the metric exists.</verify>
  <done>VenueHealth::increment_connections() emits feed_reconnections_total counter with venue label. All venues (including Derive) get reconnection tracking via Prometheus.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes with Derive pipeline wired
2. `cargo test -p prediction` passes (all tests)
3. `DeriveSupervisor` and `DeriveProcessor` are spawned in pipeline.rs
4. `forward_snapshots` connects Derive to shared snapshot fan-in channel
5. Derive pipeline block appears BEFORE `drop(snapshot_tx)`
6. `feed_reconnections_total` metric emitted in VenueHealth
7. main.rs logs Derive availability at startup
</verification>

<success_criteria>
Derive MarketSnapshots flow through run_live_multi_venue to all downstream engines without any engine modifications. Prometheus exposes all Derive metrics: feed_available, feed_latency_ms, feed_messages_total, subscription_active, subscription_activations_total, subscription_removals_total, and feed_reconnections_total.
</success_criteria>

<output>
After completion, create `.planning/phases/32-pipeline-wiring-and-observability/32-02-SUMMARY.md`
</output>
